// デバッグモード（クイズ設定の実行時上書き）に関する定数

/** answerTimeLimit 上書き値の下限（秒） */
export const DEBUG_ANSWER_TIME_LIMIT_MIN = 1

/** answerTimeLimit 上書き値の上限（秒） */
export const DEBUG_ANSWER_TIME_LIMIT_MAX = 300

/** maxAttempts 上書き値の下限（回） */
export const DEBUG_MAX_ATTEMPTS_MIN = 1

/** maxAttempts 上書き値の上限（回） */
export const DEBUG_MAX_ATTEMPTS_MAX = 9
