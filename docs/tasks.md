# Tasks

旧改善サイクルの主要実装は完了し、計画・実行spec・完了タスクを履歴へ移動した（2026-09-10）。直近の文書整備状況を2026-09-11に更新した。

## 3D早押しボタン（2026-09-14・ローカル実装完了）

- [x] 表示方式（2D／3D）→3Dのボタンタイプ（丸型／箱型（大ランプ））の階層UIと保存・復元
- [x] 3Dモデル上の文字表示なし、WAITは減光・消灯。既存ゲーム判定と100ms遷移を維持
- [x] クリックと回転の分離、最低44pxの操作領域、遮蔽判定、2Dフォールバック、資産解放
- [x] 622テスト・カバレッジ全指標80%以上・型チェック・Lint・両環境のビルド・オンライン監査0件
- [x] ローカルChromiumで両モデル・WAIT・320px・設定UI・WebGL障害時の復帰を確認
- [ ] develop公開後、実YouTube・iOS／Android・読み上げ・PWA更新を確認

詳細・検証条件・サイズは[組み込み計画](button-3d-integration-plan.md)を参照。初期表示は2D。push・本番公開は未実施。

## Cloudflare Actions移行（2026-09-14・ローカル検証完了）

- [x] Direct Uploadプロジェクト`youtube-quiz-battle`の作成と認証情報の登録をユーザーから確認
- [x] Quality checksをPR・main・developへ適用し、検証成功後の同一artifactだけをCloudflareへ公開する構成に変更
- [x] 旧GitHub Pagesのdeploy workflowを削除。Pagesの本番ブランチがmainでない場合は公開を停止
- [x] mainは本番GA4、develop・PRはGA4無効にするビルド設定を追加
- [x] 全591テスト、カバレッジ全指標80%以上、型チェック、Lint、両環境のビルド成功
- [x] ビルド成果物のGA4 ID有無、YAML構文、検証ジョブへの認証情報非提供、本番ブランチ確認処理の成功・異常系を確認
- [ ] developへpushしてActionsの認証・アップロードと固定HTTPS URLを確認
- [ ] mainへ反映し本番公開、独自ドメイン設定、旧GitHub PagesのUnpublish

アカウントIDはRepository variable／secretの両方に対応。初回のリモート実行は未実施。設定と手順は[デプロイ手順](deployment.md)を参照。

## Cloudflare移行準備：ルート配信（2026-09-14完了）

- [x] Viteの公開パスを`/`に変更し、PWAのid/start_url/scopeとアイコンURLを同期
- [x] 開発・実機確認のURLと設計書を更新。旧公開URLは移行前の公開先として保持
- [x] 588テスト・全指標80%以上のcoverage・型チェック・本番ビルド・Lint成功
- [x] 生成物のHTML・アイコンの参照実在と、JS/CSS/Service Workerに旧サブパスがないことを検証
- [x] Cloudflareのプロジェクト作成、ActionsによるDirect Uploadの実装、環境別GA4（上記参照）
- [ ] Actionsでの初回公開、本番ドメイン設定

アップロード対象は`dist/`の内容。ルート配信変更時点では旧workflowを保持していたが、上記の後続作業でCloudflare向けへ変更済み。push・公開は未実施。

## 動画停止と解答期限の統一（2026-09-12完了）

終端シークの追加修正`5f449d5`はコミット済み。次の対応として「設定・テーマ選択・横画面警告中は動画を停止し、解答期限は延長しない」を実装した。

- [x] 停止理由を集合で管理し、設定・横画面・非表示が重なっても最後の解除まで動画を停止
- [x] 手動停止を保持し、設定からテーマ選択への切替中も再生しない
- [x] 解答中は警告表示・非表示中も期限を継続。タイマー通知の遅れと期限後の送信もtimeoutとして処理
- [x] 警告中の時間切れや開始演出の完了による動画再生要求を保留
- [x] 初期化前の表示状態を引き継ぎ、設定・テーマ・横画面警告内に制限時間の案内を追加
- [x] 全588テスト成功。coverage: Statements 90.51% / Branches 83.58% / Functions 90.78% / Lines 93.19%
- [x] 型チェック・本番ビルド・Lint・差分検査成功。`npm audit --offline`は0件（最新オンラインDB未照合）
- [ ] 実機で終端復帰と新しい停止ポリシーを再確認。push・再デプロイは未実施

確認時は、解答中に設定・横画面警告を開いても時間が進むこと、時間切れになっても動画は閉じるまで止まること、設定と横画面警告を両方閉じてから再開することを確認する。

## 終端位置に留まる問題の追加修正（2026-09-12）

初回修正後、Resultには進まないが終端に留まるとの報告を受けた。実機の通知順序は未採取だが、コードには時刻通知の遅れや内部シーク待機中に終了通知を無視するだけの経路と、一度の復帰指示で成功したものとして待つ経路が残っていた。

- [x] 古い時刻でのENDED、初回seek未反映、再生指示による先頭への移動をテストで再現
- [x] 復帰先を保持し、再生/停止→seekの順に指示。既存の監視で到達確認し500ms間隔・最大10秒で再試行
- [x] 停止中・横画面警告・再生停滞との重なり、待機期限、reset/destroy時の解除を検証
- [x] 575テスト成功。coverage: Statements 90.35% / Branches 83.74% / Functions 90.20% / Lines 92.94%
- [x] 型チェック・本番ビルド・Lint・差分検査成功。`npm audit --offline`は0件
- [ ] 実YouTubeで修正後の終端復帰を再確認（再デプロイ未実施）

この時点では停止ポリシーは未変更（上記の後続対応で実施済み）。

## 終端シークの修正（2026-09-12）

HTTPS配信で、シーク許可OFFでも動画末尾へ移動するとResultへ進む問題が報告された。ENDEDイベントが通常のシーク禁止判定を通らず残問を確定していたため、終了確定前にゲーム側の判定を追加した。

- [x] 禁止された終端シークでは元の位置へ戻し、結果・解答状態を維持する
- [x] 再生中・手動停止中・解答中、内部シーク中の遅延通知、自然終了、許可ON、開始前の通知を検証
- [x] 全566テスト成功。coverageはStatements 90.35% / Branches 83.56% / Functions 90.14% / Lines 92.98%
- [x] 型チェック・本番build・Lint・差分検査成功。`npm audit --offline`は0件（最新オンラインDB未照合）
- [ ] 実YouTube・実機での修正後の終端シーク再確認

この修正はローカル検証まで。再デプロイは未実施。設定画面・横画面警告の停止ポリシーは変更していない。「動画は停止、解答カウントダウンは継続」の案は上記の後続対応で実施済み。

## Fの実施記録（2026-09-11・ローカル作業完了）

単体作業で未使用資産の整理、カバレッジ最終ゲート、文書同期とローカルPWA確認を実施した。実機項目は下表のとおり未確認であり、リリース前の確認として残す。

- [x] import・URL・globの参照がない`src/assets/base.css`と`src/assets/logo.svg`を削除。アイコン原画像や公開用資産は維持
- [x] 全ソースcoverageのStatements/Branches/Functions/Lines下限をすべて80%へ引き上げ
- [x] README・設計・計画を同期し、[実機確認手順](local-device-testing.md#リファクタリング後の実機チェック)を整備
- [x] 本番ビルドのPWA登録・資産配信・オフライン再表示をローカルChromiumで確認

### Fの検証結果

| 項目 | 結果 |
|---|---|
| Vitest | 41ファイル・559件成功 |
| 全ソースcoverage | Statements 90.39% / Branches 83.36% / Functions 90.26% / Lines 92.99%。新しい80%下限を通過 |
| 型チェック・本番build・非破壊Lint・`git diff --check` | 成功。型チェックとbuild-onlyも個別実行 |
| PWA生成 | Service Worker・Manifestを生成。事前キャッシュ23件 |
| ローカルChromium | 390×844、外部通信遮断。正しいサブパスへService Workerを登録し、Manifestのscope/start_urlとアイコン・クイズデータの配信を確認 |
| オフライン | 登録後の再読み込みでアプリの枠組みとsampleデータをキャッシュから取得。外部遮断時にYouTube読み込み失敗のエラー表示を確認。未処理JavaScript例外0件 |
| `npm audit --offline` | 0件。最新オンラインDBとの照合は未実施 |

ブラウザ確認には環境内の既存Chromiumと一時スクリプトを使用した。リポジトリへのE2Eスイート・依存追加は行っていない。

### リリース前の未確認項目

| 項目 | 状態 |
|---|---|
| iOS/AndroidのIME・キーボード・音声unlock、実YouTubeの進行・シーク・復帰・2周目 | 未確認。端末での操作が必要 |
| VoiceOver/TalkBack・実機キーボードでのフォーカス移動と復元 | 未確認。対象端末・支援技術での確認が必要 |
| PWAの更新保留中のプレイ、新ビルドへの更新・再起動、ホーム画面からの起動 | 未確認。同一URLで新旧ビルドを配信する確認が必要 |
| GitHub Actionsのリモート実行 | 未実施。push・デプロイは行っていない |

GA4の既存実装範囲は従来の動作確認済み判断を維持する。ルール同期・設計同期スキルは保留。

## Eの実施記録（2026-09-11完了）

単体作業でダイアログの共通制御とSettingsModalの責務分割を実施した。画面ごとの閉じられる条件、テーマとレイアウトを維持した。

- [x] 最前面のフォーカス制御・背景遮断・復元・Escapeを共通化
- [x] テーマ選択の終了とアニメーション取消を整合
- [x] 一般設定とデバッグ設定を分離して既存操作を検証
- [x] 全チェックと設計・実施記録の同期


### Eの最終チェック

| 項目 | 結果 |
|---|---|
| Vitest | 41ファイル・559件成功（E開始前550件。項目順の静的検査1件を実DOMの操作検証へ統合） |
| 全ソースcoverage | Statements 90.39% / Branches 83.36% / Functions 90.26% / Lines 92.99% |
| 型チェック・本番build・非破壊Lint・`git diff --check` | 成功 |
| `npm audit --offline` | 0件。最新オンラインDBとの照合は未実施 |

E終了時点の次対象はF（上記でローカル作業を実施）。実機/PWA更新・スクリーンリーダーでの確認は引き続き残る。push・デプロイは行っていない。


## Dの実施記録（2026-09-11完了）

Appの責務分割を単体で実施した。E・Fは対象外。iOSのfocus・warmup・unlockはユーザー操作内の同期呼び出しを維持した。

- [x] Analyticsの送信内容・回数・リプレイをApp統合テストで固定
- [x] Analyticsの監視・イベント生成・設定スナップショットを抽出
- [x] 初期化・セッション操作・音声・終了処理を抽出してライフサイクルを検証
- [x] 全チェックと設計・実行記録の同期


### Dの最終チェック

| 項目 | 結果 |
|---|---|
| Vitest | 39ファイル・550件成功（D開始前541件） |
| 全ソースcoverage | Statements 89.33% / Branches 80.38% / Functions 86.90% / Lines 91.75%。全指標80%以上 |
| 型チェック・本番build・非破壊Lint | 成功 |
| `git diff --check` | 成功 |
| `npm audit --offline` | 0件。最新オンラインDBとの照合は未実施 |

D終了時点の次対象はE（上記で完了）。Fのカバレッジ目標は数値上達成したが、未使用資産整理・実機・PWA確認は未実施。push・デプロイは行っていない。


## A〜Cの実施記録（2026-09-11完了）

[改善計画](project-review-and-refactoring-plan.md)のA〜Cを連続実施し、修正範囲ごとにコミットした。D〜Fは今回の実行対象外。追加依頼以降はサブエージェントを使わず単体で実施した。

- [x] 品質基盤: 全ソースcoverage下限、非破壊Lint、PR検証、デプロイ権限分離
- [x] 開始ゲート・overlay操作遮断とIME確定Enter
- [x] 内部シーク・0秒開始・問題別の結果記録
- [x] 継続した再生停滞の検出・復帰
- [x] Playerの初期化期限・実行時エラー・途中終了
- [x] JSON検証・保存領域が使えない場合の復旧
- [x] タイマーと音声のreset/destroy時の後始末
- [x] 全体テスト・型・Lint・build・coverage・設計同期

### 最終チェック

| 項目 | 結果 |
|---|---|
| Vitest | 38ファイル・541件成功（開始前484件） |
| 全ソースcoverage | Statements 85.93% / Branches 74.83% / Functions 82.19% / Lines 88.78%。Aの全下限を通過 |
| 型チェック・本番build | 成功 |
| `npm run lint:check`・`git diff --check` | 成功 |
| `npm audit --offline` | 0件。ローカル監査結果であり、最新オンラインDBとの照合は未実施 |
| GitHub Actions | workflowの整備まで。リモート実行・push・デプロイは未実施 |

### A〜C終了時点の残件

- D: AppのAnalytics・初期化/セッション管理の抽出（上記Dで完了）
- E: ダイアログのフォーカス閉じ込め・復元、共通基盤（F-12。上記Eで完了）
- F: 全指標80%（A〜C時点ではBranchesが未達、Dで達成）、未使用資産の整理、実機・PWAの確認
- 実機確認: iOS/AndroidのIME・音声unlock、内部シークの許可ON/OFF、停滞/タブ/横画面からの復帰、2周目、PWA更新


保留: エージェント向けルール同期、設計同期スキル。旧計画からの必須持ち越しなし。

## 直近の完了事項

- [x] READMEの利用方法、開発・検証コマンド、クイズデータ作成例、文書参照を更新
- [x] [WSLの開発画面をスマートフォンで確認する手順](local-device-testing.md)を追加し、READMEから参照
- [x] 旧改善計画の完了整理、履歴移動、参照更新

旧計画の整理方針: 設計書分割・クイズ作成スキルは不要。ルール同期・設計同期スキルは保留。GA4はアプリ実装範囲の動作確認済みとして終了し、必須残件を新計画へ持ち越さない。

| 文書 | 用途 |
|---|---|
| [全体レビューとリファクタリング計画](project-review-and-refactoring-plan.md) | A〜E実装・Fのローカル作業完了。実機・PWA更新等の未確認項目を明記 |
| [Phase R/3/D/4のタスク履歴](archive/improvement-202606/tasks.md) | 実施済み作業と当時の確認記録 |
| [Phase 1〜2のタスク履歴](archive/improvement-202606/tasks-phase1-2.md) | 初期実装の完了記録 |
| [旧改善計画の索引](archive/improvement-202606/00-overview.md) | 過去の計画・spec・裁定 |
履歴資料に残る未チェック欄や古い「次のタスク」を根拠に、自動で着手しない。
